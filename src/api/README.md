# API Layer

Admin API and Check Permission (PDP) REST routes.

- `admin/` — Tenants, Roles, Permissions, Users, Groups, Delegations, Resource Registry, Policy Import
- Check routes (PDP) in `pdp/` or `api/check.ts` per implementation
