# SDK Packages

Node, Go, Java client libraries for Check Permission API.

- `check(userId, resource, action, options?)`
- Options: tenant, scope, timeout, retry
- Optional in-memory cache (TTL ~10s)
